export interface AppEvent {
  title: string;
  price: number;
  id: string;
  name: string;
  host:string;
  description: string;
  date: string; // Add any other properties you need
  liked: boolean;
}
